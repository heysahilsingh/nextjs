type Props = {}

const SampleComp = (props: Props) => {

  const handleReport = (a: number, b: string) => {
    if( b === "String") Number(b)
  
  }

  return (
    <div>SampleComp</div>
  )
}

export default SampleComp